import React, { Component } from 'react';
import styles from './shared/common.module.css'; 
class Navbar extends Component {
  state = {};
  render() {
    return (
        <div className={styles.flexCenter}>Navigation bar</div>
    );
  }
}

export default Navbar;
